import { createHash } from "node:crypto";
import { NextResponse } from "next/server";
import { RACE_GAME_VERSION, RACE_LAPS, RACE_MAX_PLAYERS } from "@/game/race";
import { getRaceEntrantProfile } from "@/lib/raceEntrants";
import { verifyRaceRuntimeRequest } from "@/lib/raceRuntime";
import { getOrbRecord, orbGameType } from "@/lib/orbStore";
import { orbEndsAt } from "@/lib/orbLifecycle";
import { tryAcquireWinner } from "@/lib/upstashWinner";
export const runtime="nodejs";export const dynamic="force-dynamic";export const maxDuration=20;
type Result={schemaVersion:1;version:string;matchId:string;orbId:string;slug:string;startedAt:number;completedAt:number;participantCount:number;commitment:string;winnerWallet:string;winnerXUserId:string;winnerUsername:string;lapsCompleted:number};
export async function POST(request:Request){const raw=await request.text();if(!verifyRaceRuntimeRequest(raw,request.headers.get("x-race-timestamp"),request.headers.get("x-race-signature")))return NextResponse.json({ok:false,error:"Invalid Race authority signature"},{status:401});let body:Result;try{body=JSON.parse(raw)as Result}catch{return NextResponse.json({ok:false,error:"Invalid JSON"},{status:400})}if(body.schemaVersion!==1||body.version!==RACE_GAME_VERSION||body.lapsCompleted!==RACE_LAPS||!body.matchId||!body.orbId||!body.slug||!body.commitment)return NextResponse.json({ok:false,error:"Invalid Race result"},{status:400});const orb=await getOrbRecord(body.slug);if(!orb||orb.id!==body.orbId||orbGameType(orb)!=="race"||orb.commitment!==body.commitment)return NextResponse.json({ok:false,error:"Race Orb mismatch"},{status:404});if(orb.generatorVersion!==RACE_GAME_VERSION)return NextResponse.json({ok:false,error:"Race gameplay version does not match funded record"},{status:409});const endsAt=orbEndsAt(orb),now=Date.now();if(body.startedAt<orb.startsAt-2_000||body.completedAt<body.startedAt||body.completedAt>endsAt+5_000||now<orb.startsAt)return NextResponse.json({ok:false,error:"Race result timing is invalid"},{status:409});if(body.participantCount<2||body.participantCount>RACE_MAX_PLAYERS)return NextResponse.json({ok:false,error:"Race result bounds are invalid"},{status:409});const profile=await getRaceEntrantProfile(body.slug,body.winnerWallet);if(!profile||profile.xUserId!==body.winnerXUserId||profile.username!==body.winnerUsername)return NextResponse.json({ok:false,error:"Race winner identity is not a registered entrant"},{status:403});const canonical=JSON.stringify({schemaVersion:body.schemaVersion,version:body.version,matchId:body.matchId,orbId:body.orbId,slug:body.slug,startedAt:body.startedAt,completedAt:body.completedAt,participantCount:body.participantCount,commitment:body.commitment,winnerWallet:body.winnerWallet,winnerXUserId:body.winnerXUserId,winnerUsername:body.winnerUsername,lapsCompleted:body.lapsCompleted});const resultHash=createHash("sha256").update(canonical,"utf8").digest("hex");const lock=await tryAcquireWinner(orb.id,{slug:orb.slug,orbId:orb.id,replayId:`race:${body.matchId}`,manifestHash:orb.commitment,replayHash:resultHash,verifiedElapsedMs:Math.max(0,Math.trunc(body.completedAt-body.startedAt)),verifiedAt:new Date(body.completedAt).toISOString(),wallet:body.winnerWallet,xUserId:body.winnerXUserId,xUsername:body.winnerUsername});return NextResponse.json({ok:true,acquired:lock.acquired,winner:lock.record})}
