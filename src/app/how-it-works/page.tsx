import Link from "next/link"; export const metadata={title:"How it works"}; export default function Page(){const items=[["1","Create","Connect X and Solana, choose a standard SPL token, set the full prize, configure the game and schedule launch."],["2","Share","Orbs creates a unique public link with the prize, countdown and qualification flow so your post can build momentum."],["3","Race","At the committed time, every qualified player receives the same hidden-until-launch game. Skill determines the first valid finish."],["4","Claim","The winning wallet receives a secure claim flow. The final payout comes from the isolated on-chain Orb vault."],["5","Keep playing","After a winner exists, everyone else can continue the maze for fun instead of being kicked out."],["6","Drop another Orb","Players become future hosts, winners share their result, and the next funded game starts the loop again."]];return <div className="page"><div className="container"><span className="eyebrow">Simple by design</span><h1 className="page-title">One prize. One link. <span className="gradient-text">One live race.</span></h1><p className="page-intro">Orbs turns a funded token reward into a timed social event your community can actually participate in.</p><div className="steps3" style={{marginTop:34}}>{items.map(([n,t,b])=><div className="card" key={n}><div className="step">{n}</div><h3>{t}</h3><p>{b}</p></div>)}</div><div style={{marginTop:30}}><Link className="btn-primary" href="/create">Create an Orb</Link></div></div></div>}
