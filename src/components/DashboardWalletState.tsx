"use client"; import {useWallet} from "@solana/wallet-adapter-react"; import ConnectWallet from "@/components/ConnectWallet"; import Link from "next/link"; export default function Dashboard(){const{connected,publicKey}=useWallet();if(!connected||!publicKey)return <div className="card empty"><h3>Connect your wallet.</h3><p>Your hosted Orbs, winnings, and completed games will be indexed to your connected wallet.</p><ConnectWallet/></div>;const a=publicKey.toBase58();return <><div className="dashboard"><div className="metric"><span>Hosted</span><strong>0</strong></div><div className="metric"><span>Upcoming</span><strong>0</strong></div><div className="metric"><span>Wins</span><strong>0</strong></div><div className="metric"><span>Wallet</span><strong>{a.slice(0,4)}…{a.slice(-4)}</strong></div></div><div className="card empty"><h3>Your Orb history will live here.</h3><p>The indexer and analytics backend are intentionally not connected yet. The page is ready for Hosted, Upcoming, Winnings and Completed data.</p><Link className="btn-primary" href="/create">Create an Orb</Link></div></>}
