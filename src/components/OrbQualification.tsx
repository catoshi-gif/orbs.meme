"use client"; import {useWallet} from "@solana/wallet-adapter-react"; import ConnectWallet from "@/components/ConnectWallet"; export default function OrbQualification(){const{connected}=useWallet();return <aside className="card qualify"><span className="eyebrow">Before you play</span><h3>Qualify for this Orb.</h3><p className="muted">Complete these steps before the countdown reaches zero.</p><div className="q-list"><div className="q-row"><span className="q-num">1</span><div><strong>Connect X</strong><small>OAuth next phase</small></div></div><div className="q-row"><span className="q-num">2</span><div><strong>Follow the host</strong><small>Open X + attest after returning</small></div></div><div className={`q-row ${connected?"ready":""}`}><span className="q-num">{connected?"✓":"3"}</span><div style={{flex:1}}><strong>Connect wallet</strong><small>{connected?"Wallet ready":"Required for winner identity"}</small></div>{!connected?<ConnectWallet compact/>:null}</div><div className="q-row"><span className="q-num">4</span><div><strong>Human check</strong><small>Turnstile next phase</small></div></div></div><button className="btn-primary" disabled style={{width:"100%",marginTop:16}}>Game opens at launch</button></aside>}
